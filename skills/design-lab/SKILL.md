---
name: design-lab
description: 三合一统一分析/设计。单命令 /design-lab <需求> 走完 需求澄清→领域设计(架构/智能体)→GitHub 复用评估(问题拟合+裁剪)→前模拟→可选对抗→闭环报告。触发:复杂/多模块/需调研的功能/架构设计/智能体设计。简单需求(单页面/单接口/改文案)不触发。
argument-hint: <需求描述>
---

# Design Lab

单命令走完「需求 → 设计 → 复用 → 验证 → 闭环」。

**三合一融合模型**：以 deep-analysis 的调研闭环为主干，把 arch-designer 的知识注入「复用评估」环节（判断能复用/怎么复用/砍哪些），agent-designer 知识按需条件加载（无智能体不触发）。

## 触发判断

复杂需求才执行，满足任一即复杂：
- 多模块 / 需要架构决策
- 涉及外部集成（支付/登录/第三方 API）
- 涉及智能体（agent/AI 助手/工具调用/多智能体/RAG）
- 需要调研竞品 / 复用候选 / 技术选型
- 存在规模/并发/安全风险
- 用户说"做个 XX 系统/平台"，范围模糊

**简单需求不执行**，直接给 1 句方案。

## 流程总览

```
/design-lab <需求>
  ├─ Step 1  需求澄清(自适应) ── 用户确认
  ├─ Step 2  领域设计(先设计后找码) ★产出「我们的问题清单」
  │          ├─ 架构/系统类 → Read flows/arch-flow.md (场景估算/模式选型/结构/韧性)
  │          └─ 含智能体    → 追加 Read flows/agent-flow.md (BDD/模式/结构/护栏测试)
  ├─ Step 3  Read stages/researcher.md → GitHub 找复用 + 问题拟合四问 + 裁剪清单
  ├─ Step 4  Read stages/simulator.md  → 前模拟找遗漏(预读共享踩坑库)
  ├─ Step 5  Read stages/redteam.md    → 可行性整合 + 可选对抗(用户选)
  ├─ Step 6  Read stages/closure.md    → 闭环草案 ── 用户确认
  └─ Step 7  后模拟验证 → 确认后写 CLOSURE.md
        ⤴ 任意时刻冒出新需求 → 暂停，按硬规则 6 处理
```

## Step 1: 需求澄清 + 路径选择

⚠️ **收到新需求先停问路径**（得到回答前不调研、不写方案）：

> **"直接给方案，还是先调研再给方案？"**

- 选「直接给」→ 跳过调研/复用，凭经验直接给方案（标「LLM 推测」），仍走澄清
- 选「查询」→ 走完整流程

**澄清自适应**：需求清楚 → 少问甚至不问；模糊 → 问 1-2 个关键问题（平台？规模？已有代码还是全新？是否含智能体？）。锁定范围产出 `phase0`（JSON 见 `stages/simulator.md`），用户确认。

## Step 2: 领域设计（先设计，后找码）

按需求类型加载对应流程，**先设计出问题清单，再带着问题去找码**（问题拟合才有依据）：

- **架构/系统类** → **Read `flows/arch-flow.md`**：场景与信封估算 → 模式选型 → 结构设计 → 韧性设计
- **含智能体** → 追加 **Read `flows/agent-flow.md`**：BDD 场景 → 模式选型 → 结构设计 → 护栏与测试规划

★ **核心产出：「我们的问题清单」**——本系统/智能体碰了哪些容易出事的问题，每条对应 `references/03-problem-solution-map.md` 的哪条问题。**Step 3 researcher 拿这份清单做问题拟合**。

⚠️ **强制收尾（必须问，答完才进 Step 3）**：

> **"设计草案 + 问题清单 OK 吗？确认后我才进 Step 3 找复用。"**

## Step 3: 复用评估（researcher）

**Read `stages/researcher.md`**。带问题清单 GitHub 找具体复用，每个候选过**问题拟合四问**（它解决什么问题 / 我们需求里真有吗 / 解法对症吗 / 怎么复用）+ **裁剪清单**（保留/砍掉/改掉）。

⚠️ **强制收尾（必须问，答完才进 Step 4）**：

> **"复用评估 + 裁剪清单 OK 吗？确认后我才进 Step 4 前模拟。"**

## Step 4: 前模拟（找遗漏）

**Read `stages/simulator.md`**。基于 phase0 + Step 2 设计 + Step 3 researcher 产物，扫三类遗漏（用户没提但可能需要的 / 用户想当然的假设 / 同类项目踩坑），预读共享踩坑库 `references/04-pitfalls.md`。

## Step 5: 整合 + 可选对抗

⚠️ **前置硬约束：必须先完成 Step 3 复用评估 + Step 4 前模拟（拿到复用候选 Top3 + 遗漏清单），才允许问下面的对抗收尾问题。前模拟没做完，不许问「要不要跑对抗」——对抗以遗漏清单为输入，顺序不可颠倒。**

**Read `stages/redteam.md`**。先可行性整合（复用候选 Top3+裁剪 / 遗漏 Top5 / 裁决 proceed/改/转向/kill）。

⚠️ **强制收尾问题（必须问，答完才进 Step 6）**：

> **"方向对吗？可行性 OK 吗？要不要跑对抗 red-team？（跑 / 跳过）"**

即使用户回答「直接下一步」/「确认」，**也必须追问对抗决策**（跑 / 跳过），得到明确回答后才进 Step 6。

- 选「跑」→ 执行三步合一完整性对抗（Steelman 完整版 → Actor 穷举 4 问 → Premortem 失败倒推）。⚠️ 对抗结果必须获用户确认后才进 Step 6。
- 选「跳过」→ 直接进 Step 6。

## Step 6: 闭环草案

**Read `stages/closure.md`** 发草案（骨架 + 复用施工单 + 路线图 + 自研施工单）。

⚠️ **强制收尾**：发完草案必须问用户：

> **"方案 OK 吗？确认后我跑后模拟验证。"**

## Step 7: 后模拟 + 写闭环

用户确认草案后跑**后模拟**（模拟执行计划，走不通就修）。⚠️ 走查结果确认后才写 `CLOSURE.md`。

## 硬规则

1. **复杂需求才触发** — 简单需求直接忽略
2. **复用优先** — 选「查询」时先搜 GitHub，有现成直接引用，不从零想方案；选「直接给」时不搜索，凭经验直接给（标「LLM 推测」）
3. ★**复用候选必须过「问题拟合四问」+ 出裁剪清单**；无复用的坑必须写解决方案，不能只列不留
4. ★**无智能体的需求不加载 agent 知识**（不触发 agent-flow，避免无关内容）
5. **每条声明标注来源** — GitHub 代码(repo@文件:行号) 或 「LLM 推测」
6. **需求变更即停** — 任意时刻冒出新需求（流程中/闭环报告后）：暂停 → 记录 → 评估影响 → 回退对应 Step（动范围→Step1、动设计→Step2、仅追加→闭环报告需求池）→ 用户确认后继续。绝不默默按旧需求做到底
7. **问完必停** — 每次向用户提问（路径选择/澄清/强制收尾）后必须停下等待回答，得到答复前绝不推进下一步。后模拟之后必须先问确认，确认后才写 CLOSURE.md

## 目录结构

```
~/.claude/skills/design-lab/
├── SKILL.md              # 本文件: 调度入口 + Step 1-7 编排 + 硬规则
├── stages/
│   ├── researcher.md      # Step 3: GitHub 调研 + ★问题拟合四问 + 裁剪清单
│   ├── simulator.md       # Step 4: phase0 + 前模拟遗漏扫描（预读踩坑库）
│   ├── redteam.md         # Step 5: 可行性整合 + 可选对抗
│   └── closure.md         # Step 6-7: 闭环草案 + 后模拟 + 写文件
├── flows/
│   ├── arch-flow.md       # Step 2 架构设计（场景估算/模式选型/结构/韧性）
│   └── agent-flow.md      # Step 2 智能体设计（条件触发，无智能体不加载）
├── references/            # 共享参考库（arch + agent 合并）
│   ├── 01-patterns.md     # 9 大架构模式 + 选型口诀
│   ├── 02-agent-patterns.md # 智能体设计模式（20+）
│   ├── 03-problem-solution-map.md # ★合并问题→方案映射（架构5组+智能体14类）
│   ├── 04-pitfalls.md     # 合并踩坑库（架构#01-17 + 智能体#A01-A38 + 事故）
│   ├── 05-checklists.md   # 合并检查清单 + 评审标准 + 交付门禁
│   ├── 06-templates-map.md # 31 真实系统模板地图
│   ├── 07-agent-templates.md # agent 模板（prompt/schema/测试）
│   ├── 08-design-method.md # 架构方法（八步/信封估算/ADR/演进/经验库）
│   └── 09-delivery.md     # agent 交付（评估/护栏/监控）
└── experience/            # 按领域分开保留
    ├── arch.md            # 架构经验
    ├── agent.md           # 智能体经验
    └── general.md         # 通用调研经验
```
